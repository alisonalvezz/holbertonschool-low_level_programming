#ifndef HEADER_FILE
#define HEADER_FILE
#include "main.h"
#endif

/**
 * _abs - The absolute valor
 *
 * @n: the integer
 *
 * Return: the absolute value of the integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

		return (-n);
}
