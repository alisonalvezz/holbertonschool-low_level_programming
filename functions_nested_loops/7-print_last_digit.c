#ifndef HEADER_FILE
#define HEADER_FILE
#include "main.h"
#endif

/**
 * print_last_digit - print last digit
 *
 * @n: digit
 *
 * Return: the last digit
 */

int print_last_digit(int n)
{
	if ((n % 10) <= 0)
	{
		return (n);
	} return (-n);
}
